# Inspector Environment Configuration
# Copy this file to .env in the inspector folder if needed

# Inspector Settings
INSPECTOR_PORT=5173
INSPECTOR_HOST=localhost

# Development Settings
NODE_ENV=development
DEBUG=mcp:inspector

# Optional: Custom server configurations
DEFAULT_SERVER_CONFIG=../examples/configurations/basic-filesystem.json